import Layout from "components/layout"
import * as React from "react"
import { headingStyles } from "utils/styles"

const SacramentoPage = () => (
  <Layout>
    <main>
      <h1 style={headingStyles}>Sacramento</h1>
    </main>
  </Layout>
)

export default SacramentoPage
